---
# Inventaire staging — Control Repository reverse_proxy
# VM proxy dédiée, distincte de l'ancienne vm-proxy-staging (192.168.1.56)
# du dépôt devops_staging_prod_infra.
all:
  children:

    proxy:
      hosts:
        vm-proxy-staging:
          ansible_host: 192.168.1.93
