---
# Inventaire production — Control Repository reverse_proxy
# VM proxy dédiée, distincte de l'ancienne vm-proxy-prod (192.168.1.69,
# placeholder TODO jamais résolu) du dépôt devops_staging_prod_infra.
all:
  children:

    proxy:
      hosts:
        vm-proxy-prod:
          ansible_host: 192.168.1.95
          ansible_user: devops
