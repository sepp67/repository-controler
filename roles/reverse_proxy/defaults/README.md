# roles/reverse_proxy/defaults/

Vide par construction.

Doit contenir `main.yml` avec les variables de rôle par défaut (`caddy_conf_d_dir`,
`caddy_candidate_dir`, `registry_path` dérivé de `environment_name`,
`staging_ca_endpoint_domain`) — 06B v0.2 Task T2.2.3, 04E § 7.4.
